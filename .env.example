# Copy this file to .env and fill in your keys.
# .env is gitignored — never commit real keys.

# Required for --provider openai (default)
OPENAI_API_KEY=sk-...

# Required for --provider anthropic
ANTHROPIC_API_KEY=sk-ant-...
